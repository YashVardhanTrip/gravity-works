# Gravity Works

Physics-based gravity simulation with WebGL visualization and real-time metrics.

## Setup

```bash
npm install
npm start
```

Opens on http://localhost:3000

## API

- `GET /api/metrics` — system performance data
- `GET /api/health` — service health check
